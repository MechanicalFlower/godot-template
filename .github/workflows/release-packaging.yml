# SPDX-FileCopyrightText: 2023 Florian Vazelle <florian.vazelle@vivaldi.net>
#
# SPDX-License-Identifier: MIT

name: Release Packaging

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  version: 0.1.0
  godot_version: 3.5
  export_name: Greeter

jobs:
  release-packaging:
    runs-on: ubuntu-20.04
    container:
      image: barichello/godot-ci:3.5

    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows
            output: ${{ env.export_name }}.exe
            preset: Windows Desktop

          - platform: linux
            output: ${{ env.export_name }}.x86_64
            preset: Linux/X11

          - platform: web
            output: index.html
            preset: HTML5

          - platform: mac
            output: ${{ env.export_name }}.zip
            preset: Mac OSX

    name: ${{ matrix.preset }} Export
    steps:
      - uses: actions/checkout@v3

      - name: Export ${{ env.export_name }}
        uses: ./.github/actions/export-game
        with:
          platform: ${{ matrix.platform }}
          output: ${{ matrix.output }}
          preset: ${{ matrix.preset }}
          version: ${{ env.version }}
          godot_version: ${{ env.godot_version }}

      - name: Upload artifact
        uses: ./.github/actions/upload-artifact
        with:
          name: ${{ env.export_name }}-${{ matrix.platform }}-v${{ env.version }}
          path: build/${{ matrix.platform }}

      # Installing rsync is needed in order to deploy to GitHub Pages. Without it, the build will fail.
      - name: Install rsync
        if: ${{ matrix.platform == 'web' }}
        run: |
          apt-get update && apt-get install -y rsync

      - name: Deploy to GitHub Pages
        if: ${{ matrix.platform == 'web' }}
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages # The branch the action should deploy to.
          folder: build/web # The folder the action should deploy.
